// Set this to the deployed HTTPS serverless endpoint, for example:
// https://your-project.vercel.app/api/metar
// GitHub Pages cannot host this function and relative /api URLs are prohibited.
export const METAR_PROXY_URL='';
